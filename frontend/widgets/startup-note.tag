<startup-note>
  ok
  <div each={ i in [1,2,3] }>
    {i}
  </div>


  <script>
  var self = this

  self.choice = opts.choice || 5
  self.ratio = opts.ratio || 1
  self.text = opts.text || "descrition missing"
  self.selected = null
  </script>

</startup-note>